test network partitions

test change of leader

test raft under a bunch of different node failures

Raft:
make message parsing more robust
add more error handling
try to optimise raft log maybe
make it change state based and then call any role based functions in raft_node
should not call follower append_entries if not a follower

future:
make client retry based on failed connections with different nodes
get consistent hash map from server
be able to add and remove nodes as pleased

install json parser
make client get hashmap from nodes
make all message parsing be json

make feature to add and remove nodes to the network

test throughly

create menu to interact with

refactor where nessisary

to add a node basically run server with ip/port of a node in network


running:

server:
mvn exec:java -Dexec.mainClass=runnable.StartNodes

client:
mvn exec:java -Dexec.mainClass=runnable.Client

okay so currently clusterState sends a distribute data message followed by a updateNodes message

the distribute data message should be sent straight to the state machine where it's data should be distributed accordingly, or deleted depending on weather it was the leader that sent the message, maybe add an action field where it's either distribute or send depending on if it's a leader or not

every single dict message should be sent with a version number, if message with a later cluster version is recieved the node should hold off on executing until it's version has caught up with the version of the message. 

if a node recieves a version lower than it's current version, it should respond with a config reply so that client can update it's cluster version


make sure to reply to client if they send messages for stale configerations


lost data: maybe change although consensous should still work, data is not garunteed

change so that if a node dies halfway through sending it's data, all data is still distributed properly

send a new messagetype during redistribute phase, store data in recieving shard (put through raft) and store in a seperate store
in statemachine, only commit said data to store once it's acknowledged as being complete



new message: type=Data, Key=k, Value=v, index=i, last=true|false, shard=List<Node>
new message: type=Ack, index=i

next: 

problem: what if a ndoe dies when reditributing it's data

when DistData message is recieved enter redistribute phase, if leader start redistribute, act normal otherwise
nodes should not be able to progress to next version until out of redistribute phase

note all redistributed data should have version current version + 1

this means redistribute messages could pile up, only accept if is equal to current phase + 1

after each piece of data is sent, send an ack back to Node, when a leader is elected check if in redistribute phase,
if yes start redistribute

data is held in server, and only put into raft when all data is recieved, so last = true in data message

add timeout to send data message so that if an ack is not recieved within a certain time try next node

if new node or starting redistribute start on first index, then when ack is recieved send the piece of data where index = msg.index + 1

when leader recieves ack for last message send message to all nodes in cluster signifing end of redistribute phase
in server record the version of the last shard to fully send all it's data, as only one shard per version will redistribute data
we know if version is >= to new Data messages to reply saying all data in already there


future:

problem: needs to reply with current config if client is using stale version number

problem: need to configure client properly