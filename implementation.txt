fix cluster membership change bug

add functionality for shard spliting and removal with cluster membership

for request with stale version, cluster should reply with new config

fill out test suite

fill out comments

fill out readme

bugs:

if first query is invalid freezes and dosne't get response properly

if leader dies mid config update make new leader finish config change

make some sort of controller for all the nodes so when client start a new node it sends controller a message

remove msg dosen't work correctly

next:

if client sends messages from an invalid version, reply to client and say invalid config

how too?

make client store leader of each shard as nessisary
when sends a request to node, if node is not leader send newleader response message
client updates leader of said shard or the cluster to be new leader when recieve new leader

if the version of the cluster client holds is invalid where invalid means < current version send client stale config message
on stale config message client requests new cluster state

responses, make a new message which is repliable, where a message extends this class if it can be replied too
this message holds the comm, make node check if comm = null before replying if nessisary

may need to add an additional parameter on server boolean SEED, true if seed node, determines if node starts as learner or not

error if kill leader, dosen't respond properly as message is passed onto leader but the leader node is already dead, therefore message is lost
and never gets delivered to new leader

potential error responding to client, what happens if leader nodes dies after it sends out the entry but before it commits it and responds